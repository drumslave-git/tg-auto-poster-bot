services:
  app:
    build: .
    image: ${IMAGE:-tg-auto-poster-bot}
    ports:
      - "${PORT:-3000}:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
      # The queue, the settings and the post history live in the volume below.
      DATABASE_PATH: /app/data/app.db
      # Shared secret for every /api call. Leave empty only on a private host.
      DASHBOARD_PASSWORD: ${DASHBOARD_PASSWORD:-}
      # Bootstrap values, used only the first time the database is created —
      # everything below is managed from the dashboard afterwards.
      BOT_TOKEN: ${BOT_TOKEN:-}
      ADMIN_IDS: ${ADMIN_IDS:-}
      MANAGER_IDS: ${MANAGER_IDS:-}
      TZ_NAME: ${TZ_NAME:-UTC}
      TZ: ${TZ:-UTC}
    volumes:
      # Bind-mounted host directory so the database survives a redeploy. The
      # container runs as a non-root user, so this path must be writable by it.
      - "${DATA_DIR:-./data}:/app/data"
    healthcheck:
      # 127.0.0.1 (not localhost): the server binds IPv4, and localhost can
      # resolve to IPv6 ::1 inside the container, which would refuse-connect.
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:3000/api/health >/dev/null 2>&1 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s
    restart: unless-stopped
